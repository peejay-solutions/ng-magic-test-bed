import { describeBeforeEach } from '../describe-before-each/describe-before-each.function';


describeBeforeEach('DescribeBeforeEachTest', () => {

    const x = {
        count: 0
    };

    // it('test 1', () => {
    //     x.count++;
    //     expect(x.count).toEqual(1);
    // });

    // it('test 2', () => {
    //     x.count++;
    //     expect(x.count).toEqual(1);
    // });
});
